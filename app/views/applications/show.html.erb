 <h1><%= @application.appname %></h1>
 
 <div class="row">
   <div class="col-md-8">
     <p><%= @application.url %></p>
       <%= @application.event(:appname).count %>
   </div>
     
   <div class="col-md-4">
     <%= link_to "Edit", edit_event_application_path(@application.event, @application), class: 'btn btn-success' %>
     <%= link_to "Delete Post", [@application.event, @application], method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this application?' } %>
   </div>
 </div>
   </div>
 </div>