 <h1>Edit Application</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for applications</p>
     <ul>
       <li>Make sure it rhymes.</li>
       <li>Don't use the letter "A".</li>
       <li>The incessant use of hashtags will get you banned.</li>
     </ul>
   </div>
   <div class="col-md-8">
    <%= form_for [@application.event, @application] do |f| %>
     
       <div class="form-group">
         <%= f.label :appname %>
         <%= f.text_field :appname, class: 'form-control', placeholder: "Enter post title" %>
       </div>
       <div class="form-group">
         <%= f.label :url %>
         <%= f.text_area :url, class: 'form-control', placeholder: "Enter post body" %>
       </div>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>