<h1>Edit Event</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for events:</p>
     <ul>
       <li>Make sure the topic is appropriate.</li>
       <li>Never insult dogs.</li>
       <li>Smile when you type.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @event do |f| %>
       <div class="form-group">
         <%= f.label :eventname %>
         <%= f.text_field :eventname, class: 'form-control', placeholder: "Enter topic name" %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>